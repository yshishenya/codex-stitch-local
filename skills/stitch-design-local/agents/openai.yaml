interface:
  display_name: "Stitch Design Local"
  short_description: "Generate and iterate UI with local Stitch"
  brand_color: "#1C6BFF"
  default_prompt: "Use $stitch-design-local to generate or refine a Stitch UI from my brief and save the HTML/screenshots locally."

policy:
  allow_implicit_invocation: true
